using UnityEngine;
using System.Collections;

public class OxygenSpawn : GroundSpawn {
	public GameObject a;//oxygen,clone;
	// Use this for initialization
	void Start () {
		//base.clone1 = Instantiate (obj [next], new Vector2 (3.478378f * 2f + platSpeed * t1, firstY - setStandofNull(next)), Quaternion.identity) as GameObject;
		
	}
	
	// Update is called once per frame
	void Update () {
		//this.clone = Instantiate (oxygen, new Vector2 (base.clone1.rigidbody2D.position.x,base.clone1.rigidbody2D.position.y+1f), Quaternion.identity) as GameObject;
	}
}
